[package]
name = "tools"
version = "0.5.1"
edition.workspace = true
license.workspace = true
publish = false

[features]
subscriptions = []

[dependencies]
time = { version = "0.3", features = ["formatting", "local-offset", "macros"] }
opensnitch-kernel-caps = { path = "../kernel-caps" }
