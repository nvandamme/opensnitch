[package]
name = "tools"
version = "0.5.0"
edition = "2024"
publish = false

[features]
subscriptions = []

[dependencies]
time = { version = "0.3.47", features = ["formatting", "local-offset", "macros"] }
