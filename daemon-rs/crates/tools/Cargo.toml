[package]
name = "tools"
version = "0.5.1"
edition = "2024"
publish = false

[features]
subscriptions = []

[dependencies]
time = { version = "0.3", features = ["formatting", "local-offset", "macros"] }
